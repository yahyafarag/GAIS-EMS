# 🏗️ EMS - Enterprise Maintenance System
# نظام إدارة الصيانة المؤسسي (الإصدار النهائي)

نظام متطور لإدارة عمليات الصيانة الميدانية، مصمم بمعايير عالمية (Production Ready) لدعم الشركات الكبرى ذات الفروع المتعددة.

![EMS Banner](https://via.placeholder.com/1200x400/020617/6366f1?text=EMS+Enterprise+System)

## 🌟 الميزات الحصرية

1.  **تجربة مستخدم زجاجية (Glassmorphism UI)**: تصميم عصري يعتمد على الشفافية، التمويه (Blur)، والتدرجات اللونية الداكنة.
2.  **الدعم الكامل للأوفلاين (Offline-First)**: يعمل التطبيق بكفاءة حتى عند انقطاع الإنترنت، ويقوم بمزامنة البيانات لاحقاً.
3.  **بوابة موحدة (Unified Portal)**: نقطة دخول واحدة توجه المستخدم تلقائياً بناءً على دوره (مدير، فني، مدير فرع).
4.  **نظام النماذج الديناميكية (Dynamic Forms)**: المدير يتحكم في حقول البلاغات من لوحة التحكم، وتنعكس التغييرات فوراً لدى الفنيين والفروع.

---

## 🚀 دليل التشغيل (Installation)

### المتطلبات
*   Node.js v18+
*   NPM أو Yarn

### خطوات التثبيت
```bash
# 1. تثبيت الحزم
npm install

# 2. تشغيل بيئة التطوير
npm run dev
```
افتح المتصفح على `http://localhost:5173`.

### بناء النسخة النهائية (Build)
```bash
npm run build
npm run preview
```

---

## 🔌 التكامل مع قواعد البيانات (Database Integration)

تم تصميم النظام بنمط **Dependency Injection** في طبقة البيانات. حالياً، يستخدم النظام `localStorage` للمحاكاة، ولكن يمكن التبديل لـ SQL أو Firebase بسهولة.

**خطوات التبديل لقاعدة بيانات حقيقية:**

1.  اذهب إلى ملف `services/api.ts`.
2.  استبدل الدوال الحالية (مثل `getReports`, `saveReport`) بأكواد `fetch` أو `axios` تتصل بالـ Backend الخاص بك.

مثال:
```typescript
// services/api.ts (قبل)
getReports: async () => JSON.parse(localStorage.getItem('reports'));

// services/api.ts (بعد)
getReports: async () => {
  const res = await fetch('https://api.your-company.com/reports');
  return res.json();
};
```
لا تحتاج لتغيير أي شيء في ملفات الواجهة (Components)، لأنها تعتمد على الـ `Types` والـ `ApiService` فقط.

---

## 📱 تطبيق الهاتف (PWA)

هذا المشروع مهيأ كـ Progressive Web App.
*   **Android/iOS**: عند فتح الموقع، سيظهر للمستخدم خيار "Add to Home Screen".
*   سيعمل التطبيق بملء الشاشة وبأداء قريب جداً من التطبيقات الأصلية (Native).
*   تم إعداد أيقونات التطبيق وألوان السمة (Theme Colors) في ملف `vite.config.ts`.

---

## 🏛️ هيكلية المشروع (Architecture)

*   `apps/*`: (نظرياً) يحتوي على منطق كل تطبيق فرعي.
*   `components/ui`: مكتبة المكونات الزجاجية القابلة لإعادة الاستخدام.
*   `components/form`: محرك النماذج الديناميكي.
*   `context/*`: إدارة الحالة (State Management) والتوثيق.
*   `hooks/*`: هوكات مخصصة (مثل مراقب الشبكة).
*   `pages/*`: صفحات التطبيق الرئيسية.

---
*تم التطوير بعناية فائقة لتلبية أعلى معايير الجودة والأداء.*